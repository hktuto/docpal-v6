export enum MenuType {
  table = 'master_table',
  view = 'view',
  dashboard = 'dashboard',
  folder = 'folder',
  record = 'record',
  root = 'root',
  setting = 'setting',
  detail = 'detail',
  list = 'list',
  grid = 'grid',
  form = 'form',
  card = 'card',
}
